diff --git a/xarm_planner/include/xarm_planner/xarm_planner.h b/xarm_planner/include/xarm_planner/xarm_planner.h
index 85d86f3..ce3afbf 100644
--- a/xarm_planner/include/xarm_planner/xarm_planner.h
+++ b/xarm_planner/include/xarm_planner/xarm_planner.h
@@ -42,9 +42,11 @@ namespace xarm_planner
         bool executePath(bool wait = true);
     private:
         void init(const std::string& group_name);
+        void publishPlan();
 
         rclcpp::Node::SharedPtr node_;
         std::shared_ptr<moveit::planning_interface::MoveGroupInterface> move_group_;
+        rclcpp::Publisher<moveit_msgs::msg::DisplayTrajectory>::SharedPtr display_path_publisher_;
         moveit::planning_interface::MoveGroupInterface::Plan xarm_plan_;
         moveit_msgs::msg::RobotTrajectory trajectory_;
         bool is_trajectory_;
diff --git a/xarm_planner/src/xarm_planner.cpp b/xarm_planner/src/xarm_planner.cpp
index 69c1271..32ec044 100644
--- a/xarm_planner/src/xarm_planner.cpp
+++ b/xarm_planner/src/xarm_planner.cpp
@@ -31,6 +31,9 @@ void XArmPlanner::init(const std::string& group_name)
     std::copy(move_group_->getJointModelGroupNames().begin(), move_group_->getJointModelGroupNames().end(), std::ostream_iterator<std::string>(std::cout, ", "));
     move_group_->setMaxVelocityScalingFactor(max_velocity_scaling_factor);
     move_group_->setMaxAccelerationScalingFactor(max_acceleration_scaling_factor);
+    display_path_publisher_ = node_->create_publisher<moveit_msgs::msg::DisplayTrajectory>(
+        "/display_planned_path",
+        rclcpp::QoS(rclcpp::KeepLast(1)).reliable().transient_local());
 }
 
 bool XArmPlanner::planJointTarget(const std::vector<double>& joint_target)
@@ -46,6 +50,8 @@ bool XArmPlanner::planJointTarget(const std::vector<double>& joint_target)
     success = (move_group_->plan(xarm_plan_) == moveit::core::MoveItErrorCode::SUCCESS);
     if (!success)
         RCLCPP_ERROR(node_->get_logger(), "planJointTarget: plan failed");
+    else
+        publishPlan();
     is_trajectory_ = false;
     return success;
 }
@@ -58,6 +64,8 @@ bool XArmPlanner::planPoseTarget(const geometry_msgs::msg::Pose& pose_target)
     success = (move_group_->plan(xarm_plan_) == moveit::core::MoveItErrorCode::SUCCESS);
     if (!success)
         RCLCPP_ERROR(node_->get_logger(), "planPoseTarget: plan failed");
+    else
+        publishPlan();
     is_trajectory_ = false;
     return success;
 }
@@ -70,10 +78,21 @@ bool XArmPlanner::planPoseTargets(const std::vector<geometry_msgs::msg::Pose>& po
     success = (move_group_->plan(xarm_plan_) == moveit::core::MoveItErrorCode::SUCCESS);
     if (!success)
         RCLCPP_ERROR(node_->get_logger(), "planPoseTargets: plan failed");
+    else
+        publishPlan();
     is_trajectory_ = false;
     return success;
 }
 
+void XArmPlanner::publishPlan()
+{
+    moveit_msgs::msg::DisplayTrajectory display_trajectory;
+    display_trajectory.model_id = move_group_->getRobotModel()->getName();
+    display_trajectory.trajectory_start = xarm_plan_.start_state;
+    display_trajectory.trajectory.push_back(xarm_plan_.trajectory);
+    display_path_publisher_->publish(display_trajectory);
+}
+
 bool XArmPlanner::planCartesianPath(const std::vector<geometry_msgs::msg::Pose>& pose_target_vector)
 {
     // moveit_msgs::msg::RobotTrajectory trajectory;
